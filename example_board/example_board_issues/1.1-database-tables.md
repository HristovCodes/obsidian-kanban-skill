# 1.1 — Create Database Tables

**Status:** done
**Files:** `database/migrations/001-users.sql`

## What to build

Create the `users` and `sessions` tables.

### `users` table

| Column | Type | Notes |
|--------|------|-------|
| `id` | INT AUTO_INCREMENT PK | |
| `email` | VARCHAR(255) UNIQUE | |
| `password_hash` | VARCHAR(255) | bcrypt |
| `name` | VARCHAR(100) | |
| `created_at` | DATETIME | |

### `sessions` table

| Column | Type | Notes |
|--------|------|-------|
| `id` | CHAR(36) PK | UUID |
| `user_id` | INT FK → users | |
| `expires_at` | DATETIME | |

## Notes

- Add index on `sessions.user_id` for join performance
- Set `expires_at` default to 24 hours from creation
